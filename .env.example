# Example environment variables for GymFlex (DO NOT commit real secrets)

# Django
# Use a long random string in your real .env
SECRET_KEY=change-me

# Optional: set to 1 to help troubleshoot locally (do not enable in production)
# DEBUG=1

# Local Postgres (optional)
# DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DBNAME

# Frontend
# If you're running the React dev server locally, point it at your Django API
# VITE_API_URL=http://localhost:8000/api
