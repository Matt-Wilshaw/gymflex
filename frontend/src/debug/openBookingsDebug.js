// Previously a dev-only diagnostic helper. Removed logging to avoid
// noisy console output in development and production builds.

// No-op export retained so any dynamic imports succeed without side effects.
export default {};
